---
phase: 01-person-centric-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/twenty-consultancy/src/fields/employment-type.field.ts
  - packages/twenty-consultancy/src/fields/employment-status.field.ts
  - packages/twenty-consultancy/src/fields/visa-type.field.ts
  - packages/twenty-consultancy/src/fields/pay-rate.field.ts
  - packages/twenty-consultancy/src/fields/bill-rate.field.ts
  - packages/twenty-consultancy/src/fields/start-date.field.ts
  - packages/twenty-consultancy/src/fields/placed-at.field.ts
  - packages/twenty-consultancy/src/fields/recruiter.field.ts
  - packages/twenty-consultancy/src/fields/bench-since.field.ts
autonomous: true

must_haves:
  truths:
    - "Person records display 9 new HRMS fields with correct types (3 SELECT, 2 NUMBER, 2 DATE_TIME, 2 TEXT)"
    - "Employment Type SELECT shows W2, 1099, C2C options"
    - "Employment Status SELECT shows Active, On Bench, On Leave, Onboarding, Terminated options"
    - "Visa Type SELECT shows all 11 visa types (H1B, H4 EAD, L1A, L1B, F1 OPT, STEM OPT, TN, O1, Green Card, US Citizen, EAD)"
  artifacts:
    - path: "packages/twenty-consultancy/src/fields/employment-type.field.ts"
      provides: "SELECT field with W2/1099/C2C options"
    - path: "packages/twenty-consultancy/src/fields/employment-status.field.ts"
      provides: "SELECT field with 5 status options"
    - path: "packages/twenty-consultancy/src/fields/visa-type.field.ts"
      provides: "SELECT field with 11 visa type options"
    - path: "packages/twenty-consultancy/src/fields/pay-rate.field.ts"
      provides: "NUMBER field for hourly pay rate"
    - path: "packages/twenty-consultancy/src/fields/bill-rate.field.ts"
      provides: "NUMBER field for hourly bill rate"
    - path: "packages/twenty-consultancy/src/fields/start-date.field.ts"
      provides: "DATE_TIME field for consultant join date"
    - path: "packages/twenty-consultancy/src/fields/placed-at.field.ts"
      provides: "TEXT field for current client name"
    - path: "packages/twenty-consultancy/src/fields/recruiter.field.ts"
      provides: "TEXT field for internal recruiter name"
    - path: "packages/twenty-consultancy/src/fields/bench-since.field.ts"
      provides: "DATE_TIME field for bench start date"
  key_links:
    - from: "packages/twenty-consultancy/src/fields/*.field.ts"
      to: "packages/twenty-consultancy/src/constants.ts"
      via: "UUID imports from UUIDS and PERSON_OBJECT_ID"
      pattern: "from.*constants"
---

<objective>
Create 9 defineField() files that extend the Person standard object with consultancy-specific fields: employment type, employment status, visa type, pay rate, bill rate, start date, placed at, recruiter, and bench since.

Purpose: These fields are the core data model for tracking consultants. Every view and filter depends on these field UUIDs existing.

Output: 9 field definition files, one per field, each with `export default defineField(...)`.
</objective>

<execution_context>
@/Users/rohitgarlapati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rohitgarlapati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-person-centric-foundation/01-RESEARCH.md
@.planning/phases/01-person-centric-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SELECT fields (employment type, employment status, visa type)</name>
  <files>
    packages/twenty-consultancy/src/fields/employment-type.field.ts
    packages/twenty-consultancy/src/fields/employment-status.field.ts
    packages/twenty-consultancy/src/fields/visa-type.field.ts
  </files>
  <action>
Create the `src/fields/` directory if it does not exist. Then create these three SELECT field files.

**1. packages/twenty-consultancy/src/fields/employment-type.field.ts**

```typescript
import { defineField, FieldType } from 'twenty-sdk';
import { PERSON_OBJECT_ID, UUIDS } from '../constants';

export default defineField({
  universalIdentifier: UUIDS.fields.employmentType,
  objectUniversalIdentifier: PERSON_OBJECT_ID,
  type: FieldType.SELECT,
  name: 'employmentType',
  label: 'Employment Type',
  description: 'Consultant employment classification',
  icon: 'IconBriefcase',
  defaultValue: "'W2'",
  options: [
    {
      id: UUIDS.options.employmentType.w2,
      value: 'W2',
      label: 'W2',
      color: 'blue',
      position: 0,
    },
    {
      id: UUIDS.options.employmentType._1099,
      value: '1099',
      label: '1099',
      color: 'orange',
      position: 1,
    },
    {
      id: UUIDS.options.employmentType.c2c,
      value: 'C2C',
      label: 'C2C',
      color: 'purple',
      position: 2,
    },
  ],
});
```

**2. packages/twenty-consultancy/src/fields/employment-status.field.ts**

```typescript
import { defineField, FieldType } from 'twenty-sdk';
import { PERSON_OBJECT_ID, UUIDS } from '../constants';

export default defineField({
  universalIdentifier: UUIDS.fields.employmentStatus,
  objectUniversalIdentifier: PERSON_OBJECT_ID,
  type: FieldType.SELECT,
  name: 'employmentStatus',
  label: 'Employment Status',
  description: 'Current employment status of the consultant',
  icon: 'IconUserCheck',
  defaultValue: "'ONBOARDING'",
  options: [
    {
      id: UUIDS.options.employmentStatus.active,
      value: 'ACTIVE',
      label: 'Active',
      color: 'green',
      position: 0,
    },
    {
      id: UUIDS.options.employmentStatus.onBench,
      value: 'ON_BENCH',
      label: 'On Bench',
      color: 'yellow',
      position: 1,
    },
    {
      id: UUIDS.options.employmentStatus.onLeave,
      value: 'ON_LEAVE',
      label: 'On Leave',
      color: 'sky',
      position: 2,
    },
    {
      id: UUIDS.options.employmentStatus.onboarding,
      value: 'ONBOARDING',
      label: 'Onboarding',
      color: 'turquoise',
      position: 3,
    },
    {
      id: UUIDS.options.employmentStatus.terminated,
      value: 'TERMINATED',
      label: 'Terminated',
      color: 'red',
      position: 4,
    },
  ],
});
```

**3. packages/twenty-consultancy/src/fields/visa-type.field.ts**

```typescript
import { defineField, FieldType } from 'twenty-sdk';
import { PERSON_OBJECT_ID, UUIDS } from '../constants';

export default defineField({
  universalIdentifier: UUIDS.fields.visaType,
  objectUniversalIdentifier: PERSON_OBJECT_ID,
  type: FieldType.SELECT,
  name: 'visaType',
  label: 'Visa Type',
  description: 'Immigration visa or work authorization type',
  icon: 'IconId',
  options: [
    {
      id: UUIDS.options.visaType.h1b,
      value: 'H1B',
      label: 'H-1B',
      color: 'blue',
      position: 0,
    },
    {
      id: UUIDS.options.visaType.h4Ead,
      value: 'H4_EAD',
      label: 'H-4 EAD',
      color: 'sky',
      position: 1,
    },
    {
      id: UUIDS.options.visaType.l1a,
      value: 'L1A',
      label: 'L-1A',
      color: 'purple',
      position: 2,
    },
    {
      id: UUIDS.options.visaType.l1b,
      value: 'L1B',
      label: 'L-1B',
      color: 'pink',
      position: 3,
    },
    {
      id: UUIDS.options.visaType.f1Opt,
      value: 'F1_OPT',
      label: 'F-1 OPT',
      color: 'orange',
      position: 4,
    },
    {
      id: UUIDS.options.visaType.stemOpt,
      value: 'STEM_OPT',
      label: 'STEM OPT',
      color: 'yellow',
      position: 5,
    },
    {
      id: UUIDS.options.visaType.tn,
      value: 'TN',
      label: 'TN',
      color: 'turquoise',
      position: 6,
    },
    {
      id: UUIDS.options.visaType.o1,
      value: 'O1',
      label: 'O-1',
      color: 'red',
      position: 7,
    },
    {
      id: UUIDS.options.visaType.greenCard,
      value: 'GREEN_CARD',
      label: 'Green Card',
      color: 'green',
      position: 8,
    },
    {
      id: UUIDS.options.visaType.usCitizen,
      value: 'US_CITIZEN',
      label: 'US Citizen',
      color: 'gray',
      position: 9,
    },
    {
      id: UUIDS.options.visaType.ead,
      value: 'EAD',
      label: 'EAD',
      color: 'pink',
      position: 10,
    },
  ],
});
```
  </action>
  <verify>
Verify each file exists and has `export default defineField`:
- `grep 'export default defineField' packages/twenty-consultancy/src/fields/employment-type.field.ts`
- `grep 'export default defineField' packages/twenty-consultancy/src/fields/employment-status.field.ts`
- `grep 'export default defineField' packages/twenty-consultancy/src/fields/visa-type.field.ts`

Verify option counts:
- `grep -c "value:" packages/twenty-consultancy/src/fields/employment-type.field.ts` should return 3
- `grep -c "value:" packages/twenty-consultancy/src/fields/employment-status.field.ts` should return 5
- `grep -c "value:" packages/twenty-consultancy/src/fields/visa-type.field.ts` should return 11
  </verify>
  <done>
Three SELECT fields created with correct option counts (3 employment types, 5 statuses, 11 visa types), each with proper colors, labels, and positions. All UUIDs imported from constants.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create NUMBER, DATE_TIME, and TEXT fields (pay rate, bill rate, start date, placed at, recruiter, bench since)</name>
  <files>
    packages/twenty-consultancy/src/fields/pay-rate.field.ts
    packages/twenty-consultancy/src/fields/bill-rate.field.ts
    packages/twenty-consultancy/src/fields/start-date.field.ts
    packages/twenty-consultancy/src/fields/placed-at.field.ts
    packages/twenty-consultancy/src/fields/recruiter.field.ts
    packages/twenty-consultancy/src/fields/bench-since.field.ts
  </files>
  <action>
Create these six field files in the `src/fields/` directory (should already exist from Task 1).

**1. packages/twenty-consultancy/src/fields/pay-rate.field.ts**

```typescript
import { defineField, FieldType } from 'twenty-sdk';
import { PERSON_OBJECT_ID, UUIDS } from '../constants';

export default defineField({
  universalIdentifier: UUIDS.fields.payRate,
  objectUniversalIdentifier: PERSON_OBJECT_ID,
  type: FieldType.NUMBER,
  name: 'payRate',
  label: 'Pay Rate',
  description: 'Hourly rate paid to the consultant (USD)',
  icon: 'IconCurrencyDollar',
});
```

**2. packages/twenty-consultancy/src/fields/bill-rate.field.ts**

```typescript
import { defineField, FieldType } from 'twenty-sdk';
import { PERSON_OBJECT_ID, UUIDS } from '../constants';

export default defineField({
  universalIdentifier: UUIDS.fields.billRate,
  objectUniversalIdentifier: PERSON_OBJECT_ID,
  type: FieldType.NUMBER,
  name: 'billRate',
  label: 'Bill Rate',
  description: 'Hourly rate charged to the client (USD)',
  icon: 'IconReceipt',
});
```

**3. packages/twenty-consultancy/src/fields/start-date.field.ts**

```typescript
import { defineField, FieldType } from 'twenty-sdk';
import { PERSON_OBJECT_ID, UUIDS } from '../constants';

export default defineField({
  universalIdentifier: UUIDS.fields.startDate,
  objectUniversalIdentifier: PERSON_OBJECT_ID,
  type: FieldType.DATE_TIME,
  name: 'startDate',
  label: 'Start Date',
  description: 'Date when the consultant joined the organization',
  icon: 'IconCalendarEvent',
});
```

**4. packages/twenty-consultancy/src/fields/placed-at.field.ts**

```typescript
import { defineField, FieldType } from 'twenty-sdk';
import { PERSON_OBJECT_ID, UUIDS } from '../constants';

export default defineField({
  universalIdentifier: UUIDS.fields.placedAt,
  objectUniversalIdentifier: PERSON_OBJECT_ID,
  type: FieldType.TEXT,
  name: 'placedAt',
  label: 'Placed At',
  description: 'Current client name where consultant is placed',
  icon: 'IconBuilding',
});
```

**5. packages/twenty-consultancy/src/fields/recruiter.field.ts**

```typescript
import { defineField, FieldType } from 'twenty-sdk';
import { PERSON_OBJECT_ID, UUIDS } from '../constants';

export default defineField({
  universalIdentifier: UUIDS.fields.recruiter,
  objectUniversalIdentifier: PERSON_OBJECT_ID,
  type: FieldType.TEXT,
  name: 'recruiter',
  label: 'Recruiter',
  description: 'Internal recruiter managing this consultant',
  icon: 'IconUserSearch',
});
```

**6. packages/twenty-consultancy/src/fields/bench-since.field.ts**

```typescript
import { defineField, FieldType } from 'twenty-sdk';
import { PERSON_OBJECT_ID, UUIDS } from '../constants';

export default defineField({
  universalIdentifier: UUIDS.fields.benchSince,
  objectUniversalIdentifier: PERSON_OBJECT_ID,
  type: FieldType.DATE_TIME,
  name: 'benchSince',
  label: 'Bench Since',
  description: 'Date when the consultant went on bench (unplaced)',
  icon: 'IconClock',
});
```
  </action>
  <verify>
Verify all 6 files exist and have correct types:
- `grep 'FieldType.NUMBER' packages/twenty-consultancy/src/fields/pay-rate.field.ts`
- `grep 'FieldType.NUMBER' packages/twenty-consultancy/src/fields/bill-rate.field.ts`
- `grep 'FieldType.DATE_TIME' packages/twenty-consultancy/src/fields/start-date.field.ts`
- `grep 'FieldType.TEXT' packages/twenty-consultancy/src/fields/placed-at.field.ts`
- `grep 'FieldType.TEXT' packages/twenty-consultancy/src/fields/recruiter.field.ts`
- `grep 'FieldType.DATE_TIME' packages/twenty-consultancy/src/fields/bench-since.field.ts`

Count total field files: `ls packages/twenty-consultancy/src/fields/*.field.ts | wc -l` should return 9.
  </verify>
  <done>
All 9 field files exist: 3 SELECT (employment type, employment status, visa type), 2 NUMBER (pay rate, bill rate), 2 DATE_TIME (start date, bench since), 2 TEXT (placed at, recruiter). Each targets the Person object via PERSON_OBJECT_ID. All UUIDs from constants.ts.
  </done>
</task>

</tasks>

<verification>
1. 9 field files exist in packages/twenty-consultancy/src/fields/
2. Each file has exactly one `export default defineField(...)` call
3. All 3 SELECT fields have non-empty options arrays with correct counts (3, 5, 11)
4. SELECT defaultValue uses single-quote wrapping format (e.g., "'W2'")
5. All fields target PERSON_OBJECT_ID
6. No hardcoded UUIDs -- all imported from constants.ts
7. Field types match requirements: SELECT for employment type/status/visa, NUMBER for rates, DATE_TIME for dates, TEXT for text fields
</verification>

<success_criteria>
- 9 field definition files created
- Employment Type has 3 options (W2, 1099, C2C)
- Employment Status has 5 options (Active, On Bench, On Leave, Onboarding, Terminated)
- Visa Type has 11 options (H1B through EAD)
- Pay Rate and Bill Rate are NUMBER type
- Start Date and Bench Since are DATE_TIME type
- Placed At and Recruiter are TEXT type
- All UUIDs from constants.ts, none inline
</success_criteria>

<output>
After completion, create `.planning/phases/01-person-centric-foundation/01-02-SUMMARY.md`
</output>
